import { createTheme } from "@mui/material"

export const theme = createTheme({
  palette: {
    primary: {
      main: "#f44336",
      light: "skyblue",
    },
    secondary: {
      main: "#3f51b5",
    },
    otherColor: {
      main: "#ffeb3b",
    },
  },
})
